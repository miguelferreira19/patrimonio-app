# V3.md — briefing de trabalho

Este ficheiro é o contexto MÍNIMO para trabalhar na V3. Existe para ninguém ter de ler o
`PLANO.md` (74 KB) para escrever um módulo de 200 linhas. O `PLANO.md` continua a ser a
história da V2 e o conhecimento de domínio de longo prazo; este é o que está a acontecer agora.

## O que a V3 muda

A V2 entregou três superfícies (Agora, Carteira, Ano) e um vocabulário visual. Falhou em quatro
pontos, e a V3 é a correção deles:

1. **É prosa, não é visual.** Frases inteiras como títulos, cinco elementos de texto por linha de
   decisão, um parágrafo a explicar cada gráfico.
2. **Estava construída para quem a usa todos os dias.** O viewer abre a app duas vezes por ano.
3. **Não olhava para a frente.** Todo o fluxo era histórico a 12 meses.
4. **A análise da renda era fina.** Há 12 anos de série mensal na base e usavam-se 12 meses.

Fases: F0 produção (feita) · F1 papéis (feita) · F2 Agora visual · F3 `renda.ts` ·
F4 `futuro.ts` · F5 conselhos · F6 `/analise` · F7 passagem visual ao resto.

---

## As cinco regras visuais (R1–R5)

Aplicam-se a **todo** o código novo e a todo o código que se toque.

| # | Regra | Em concreto |
|---|---|---|
| **R1** | **Um número herói por superfície, não uma frase.** | O topo de uma página é `Lede` com um número em Newsreader e no máximo 8 palavras à volta. Nunca uma frase inteira como título. `PageHeader` está `@deprecated` — não usar em código novo. |
| **R2** | **`Card` só quando há elevação real.** | Agrupar com `border-t border-regua` e uma etiqueta, ou com espaço em branco. `Card` fica para overlays e para o que está mesmo em cima do papel. Uma página que é uma pilha de `Card` é a assinatura de UI gerada. |
| **R3** | **O porquê não está sempre ligado.** | Numa linha de decisão fica visível: o quê, onde, quanto. O `porque`, a `conta` e a etiqueta de confiança colapsam para `<details>`. |
| **R4** | **Nenhum parágrafo explica um gráfico.** | Se o gráfico precisa de um parágrafo, o gráfico está mal desenhado. Os eixos e a legenda são a explicação. |
| **R5** | **Etiquetas maiúsculas com moderação.** | No máximo uma etiqueta `uppercase tracking` por cada três secções da página. |

## Sistema visual "papel e tinta"

Tudo em `src/app/globals.css` (Tailwind v4, `@theme`; **não existe `tailwind.config`**). O
cabeçalho desse ficheiro explica a estratégia inteira e vale a pena ler antes de mexer em cor.

- **Regra fundadora:** o que está confirmado é **tinta**; o que exige ação é **verde-cofre**; o
  que precisa de atenção é **âmbar**; o que é perda é **vermelho**; o que é desconhecido ou
  futuro é **ardósia**. **Não há verde de "ok"** — uma carteira sã é preta sobre papel. Verde de
  sucesso em código novo está errado.
- **Código novo usa só tokens semânticos**, que trocam de par com o tema sozinhos, **zero `dark:`**:
  `bg-papel bg-carta bg-elevado bg-vellum` · `text-tinta text-tinta-2 text-tinta-3` ·
  `border-regua border-regua-forte` · `text-acao bg-acao-tenue` · `atencao` · `perda` · `futuro`.
- As classes `zinc/teal/emerald/amber/red/sky` são **legado da V1**. Não escrever classes novas
  com elas.
- Fontes: Geist (UI), Geist Mono (meses, códigos matriciais, NIF), **Newsreader** (`font-serif`)
  só em ledes, títulos do Ano e a linha de dinheiro. Nunca serifa em chrome de UI.
- Separação por **hairline** (`border-regua`), nunca por sombra. Sombra só em overlay.
- Números: `tabular-nums` sempre. **Todo o euro passa pelo `<Money>`**; percentagens e áreas pelo
  `<Figure>`.
- Movimento: tokens `--t-*` e `--e-*`. Nada acima de 420 ms, nada em loop, entrada só no mount.
- **Proibido na UI:** travessões "—", emojis, gradientes néon ou roxos, animações infinitas.

## Componentes a reusar (não reimplementar)

- `src/components/kit/` — `Money`, `Figure`, `Lede`, `Confianca`, `Cobertura`. Sem hooks.
- `src/components/faixa/` — a faixa é a **única** implementação da grelha mensal.
- `src/components/ui.tsx` — `Card`, `Button`, `buttonClass`, `Input`, `Badge`, `Table/Th/Td`,
  `EmptyState`. **NUNCA acrescentar `"use client"` a este ficheiro**: as páginas server passam
  `icon={LucideIcon}` e a diretiva cria uma fronteira de serialização que as faz crashar em
  runtime sem o build falhar. Componentes com hooks vão para ficheiro próprio.
- `src/components/charts.tsx` — Recharts. Já é dependência; não trocar de biblioteca.

---

## Armadilhas do domínio (não redescobrir)

- **`receipts.amount` é ILÍQUIDO** (coluna "Valor" do Portal); o líquido é `amount - withholding`.
  Quem guarda cash líquido é `payments.amount`. Somar os dois foi o bug B1 e está no
  `irs.check.ts` como regressão.
- **`receipts.withholding` está a 0 em toda a base** — o reimport está pendente.
- **PostgREST devolve no máximo ~1000 linhas e `.limit()` não passa por cima disso.** Usar
  `paginateAll` (`src/lib/paginate.ts`) em `payments` e `receipts` (já há >5000 linhas).
- **`futuro` ≠ `falta`.** `src/lib/monthcell.ts` é a verdade única do estado de um mês
  (`pago|parcial|falta|fora|futuro`). Um mês além da fronteira de dados NUNCA é falta — era o
  bug B2.
- **A fronteira de dados** é `dataHorizonMonth(payments)`: o último mês com pagamentos. Nada para
  lá disso é conhecido.
- **O inquilino não é uma entidade.** É `contracts.tenant_name`, texto livre, com `tenant_nif`
  frequentemente nulo. Não há chave estável. Agrupar por NIF quando existe, senão por nome
  normalizado, e declarar a incerteza — nunca fuzzy match.
- **A tabela `expenses` está praticamente vazia**: nenhum import a povoa, todas as despesas são
  manuais. Qualquer número "líquido" que a app mostre é receita menos quase nada. Declarar, não
  estimar.
- **`area_m2` está por preencher em quase todas as fichas** → `marketView().estimatedValue` e
  `grossYield` são nulos na maioria da carteira. O ecrã de preenchimento em lote está em `/admin`.
- **Ano fiscal usa `issue_date`, não `ref_month`.** Um recibo multi-mês emitido em janeiro conta
  todo no ano de emissão. Para competência usa-se `ref_month`; para IRS, `issue_date`.
- **Ótica de FAMÍLIA**: valores por inteiro ("bolo"), sem repartir por quotas. As quotas de
  `property_owners` só entram no `irs.ts`.
- **Terrenos e vendidos ficam fora de toda a métrica corrente** (`isCurrentProperty`).

## Motores que já existem — usar, não construir de novo

| Preciso de… | Já existe em |
|---|---|
| atrasos, fronteira de dados, renda de referência | `src/lib/arrears.ts` |
| contrato ativo num mês, vazios, elegibilidade de atualização, mercado, yield | `src/lib/calc.ts` |
| probabilidade de pagar (Beta-Binomial com shrinkage), curva de cura, estágios IFRS 9, perda esperada | `src/lib/portfolio/risk.ts` |
| IRS, regimes, art. 72.º, AIMI, Anexo F | `src/lib/irs.ts` |
| renda observada nos recibos | `src/lib/rent.ts` |
| o estado da carteira todo, calculado uma vez | `src/lib/portfolio/snapshot.ts` |
| o único I/O da camada de portfólio | `src/lib/portfolio/load.ts` |

Se aparecer regra de negócio nova num módulo de UI ou no snapshot, está no ficheiro errado.

---

## Ambiente e gates

- Node **não** está no PATH. Em Git Bash, prefixar sempre:
  `export PATH="/c/Users/migue/AppData/Local/Logi/LogiPluginService/PluginHosts/node22/node:$PATH"`
- **Gate obrigatório:** `npm run build` **e** `npm run check`, os dois verdes, antes de dar
  qualquer tarefa por terminada.
- `npm run check` são self-checks **puros** com `node:assert`, compilados com `tsc` para `.check/`.
  Sem base de dados, sem framework de testes. **Nunca introduzir Jest ou Vitest.** Casos novos vão
  para o `*.check.ts` do módulo respetivo.
- Um módulo novo acrescenta o seu `check:<mod>` ao script `check` do `package.json`.
- Escrita na base só por server action com `requireAdmin` (`src/lib/actions/*`).
- **Sem dependências novas.** Já existem: lucide-react, recharts, clsx, tailwind-merge, papaparse,
  xlsx.
- `dados/` tem dados pessoais reais e está no `.gitignore` — nunca commitar nem expor.
- Sem commits nem push sem pedido explícito.

---

# Estado (2026-07-29) — as duas frentes estão FEITAS em código; falta colar SQL

O que existe agora, por ordem de colagem no SQL Editor. **A ordem importa**: as despesas
espelhadas saem das quotas que a caderneta escreve.

1. **O fim do `supabase/schema.sql`**, da linha `-- V3 · DESPESAS POR SENHORIO` até ao
   fim do ficheiro. São quatro blocos: `origem` em `expenses`, a categoria `conservacao`,
   o **bug B5 do mercado** (converte os códigos de território que lá estão, não apaga) e
   as **quotas da Avó**.
2. `dados/update_cadernetas_pai.sql` — 30 frações: dicofre, freguesia, concelho, morada,
   tipologia, área, VPT, ano, e as quotas de `property_owners` a partir dos TITULARES.
   Conferir antes em `dados/cadernetas_pai.csv` e `dados/cadernetas_titulares_pai.csv`.
3. `dados/despesas_irs_2025.sql` — 108 linhas de despesa reais do Anexo F de 2025
   (Pai 6.608,92 €, Avô 36.688,04 €, Avó 2.226,89 €) mais as espelhadas do Tio.
   Conferir antes em `dados/despesas_irs_2025.csv`.

O passo 2 tem de vir antes do 3 (as despesas espelhadas do Tio saem das quotas que a
caderneta escreve). O SQL Editor só mostra o resultado da ÚLTIMA query de cada execução —
por isso são três execuções separadas, cada uma a acabar na sua query de conferência.

Cada ficheiro acaba com uma query de conferência (artigos que não casam com a base, quotas
por fração, totais por senhorio e origem). **Ler o resultado dessas queries** — é aí que
aparece uma fração com o artigo mal registado.

## Três descobertas que mudam números já existentes

- **O VPT que está na base é a QUOTA, não o valor da fração.** O CSV do Portal traz a parte
  do titular (181710-U-1887-M: 14.363,21 na base = metade dos 28.726,42 da caderneta;
  182301-R-401: 13,03 = 39,08 ÷ 3). A app assume o VPT por inteiro e aplica a quota no
  `irs.ts` (AIMI e Anexo F Q9), portanto o AIMI está subavaliado até o passo 2 ser colado.
- **As medianas do INE são de alojamentos familiares.** Com as áreas preenchidas, uma
  garagem de 276 m² a 25 €/mês entrava na mediana de yield e saía como "candidato a venda".
  `benchmarkForMetric` (calc.ts) passa a devolver `undefined` para uso não habitacional —
  uma guarda só, no sítio por onde `/mercado`, a ficha e os conselhos passam todos.
- **O código de território era o do INE, não o DICOFRE (bug B5, agora fechado).** O
  `geocod` do INE é NUTS III + DICOFRE: Viseu é `1941823` = `194` + `18` + `23`. Isso é que
  ficava em `market_benchmarks.dicofre` e no Select da ficha, enquanto a caderneta traz
  `182341` — nenhum dos dois caminhos de `benchmarkForMetric` (igualdade na freguesia,
  prefixo no concelho) podia casar, e o Mercado respondia "por saber" com a base cheia de
  benchmarks. `ine.ts` grava agora só o DICOFRE, e o SQL converte o que já existe.
  **O INE só publica ~330 freguesias e nenhuma é desta carteira**: as comparações são todas
  ao nível do CONCELHO (Viseu 6,72 €/m², Sátão 3,63 €/m² em 2026T1). Está escrito na página.
- **O Anexo F traz as despesas por FRAÇÃO e por categoria**, não só o total do ano, e os
  valores são a parte do declarante. Por isso cada linha leva `landlord_id` e
  `expenseTotalsByProperty` deixou de lhe voltar a aplicar a quota: linha com senhorio =
  a parte dele, linha sem senhorio = conta da família a repartir. Está no `irs.check.ts`.

## O que a frente B fez ao modelo

- `expenses.origem` (`registada|espelhada|estimada`) + categoria `conservacao` (a coluna
  "Conservação e manutenção" do Anexo F deduz; a `obras` genérica continua ambígua e não).
- Só `registada` chega ao Anexo F, e só a do próprio senhorio — decisão do utilizador de
  2026-07-26, agora com teste de regressão.
- `capacidadeDeInvestimento` devolve `despesasEspelhadas`; o conselho desce a confiança a
  "assumido" quando alguma entra na conta. O Ano mostra o selo linha a linha.
- Quem entrega declaração NUNCA é espelhado, mesmo nas frações onde não lançou nada: o Avô
  e a Avó entregam em conjunto e repartem entre si (ela declara o IMI, ele a conservação),
  por isso a ausência dela numa rubrica é informação, não falta de dados.

## Sessão 2026-07-29 (II) — pedidos do utilizador

- **Ficha do arrendatário** em `/inquilinos/[chave]` (admin-only), a partir de
  `src/lib/portfolio/inquilinos.ts` (módulo puro, zero queries novas: lê o snapshot).
  Clica-se no nome do inquilino na ficha da fração. A chave é a mesma de
  `concentracao().porInquilino`. Mostra contratos (ativos e cessados), recebido a 12 meses,
  dívida, estado e probabilidade de pagar. **A dívida e o estado são só dos contratos
  ATIVOS**: o snapshot só tem `arrears` do contrato ativo da fração, e atribuí-lo aos
  cessados punha a dívida do inquilino de agora na ficha do que saiu (está no check).
- **A fração deixa de mostrar o histórico de pagamentos dos contratos cessados.** Ficam na
  secção de contratos e na ficha do arrendatário.
- **O Agora**: as decisões passam a caixas em grelha (2 colunas), com os botões todos; o
  gráfico "Mês a mês" saiu (o `Fluxo` e os dois charts que só ele usava).
- **Quotas**: bloco novo no fim do `schema.sql` dá à Avó a mesma quota do Avô, com trava
  para nunca passar os 100% nas frações partilhadas com terceiros.

## Sessão 2026-07-29 (III) — a passagem visual

O sistema "papel e tinta" estava bem, a execução é que tinha duas personalidades: o
conteúdo era papel quente com serifa e hairlines, e a navegação era um rail `zinc-950` de
dashboard SaaS, com taxonomia por cima de três links. A primeira coisa que se via era a
menos pensada.

- **`masthead.tsx`** substitui o rail: cabeçalho em papel, nome em Newsreader, destinos como
  separadores que colam ao topo no scroll (no telemóvel são a única navegação, e uma
  navegação que desaparece obriga a voltar ao início da página). Admin num menu discreto à
  direita, com Esc e clique-fora. **O `nav.tsx` fica intacto** — reverter é uma linha no
  `(app)/layout.tsx`.
- **O Mercado subiu a destino de família** e foi reescrito no idioma da V3 (era a página com
  mais restos de V1: `PageHeader`, grelha de `StatCard`, classes zinc/teal cruas).
- **O Agora foi reordenado, não repintado.** Tinha QUATRO aberturas com o mesmo peso —
  número herói, cobertura, retrato de seis números, e só então o conteúdo. A ordem passa a
  ser: abertura (número + curva do ano lado a lado, 5/7), corpo, contexto, honestidade. A
  curva estava no fim da página e é o objeto mais legível que lá existe. As caixas de
  decisão abrem com o valor em euros e levam uma régua de 2px à esquerda (acao/perda).

Nenhuma cor nova, nenhum número mudado: o problema era a ordem por que a página falava.

## Auditoria de 2026-07-29 (IV) — sete achados, todos fechados

Feita a pedido do utilizador, antes de mostrar a app ao Tio. Três dos sete eram meus, das
sessões anteriores deste mesmo dia.

1. **`/senhorios` mostrava o dobro das despesas de cada senhorio.** As colunas por titular
   somavam o valor INTEIRO de cada fração à linha de CADA co-titular. Latente enquanto
   `expenses` estava vazia; o import do Anexo F acordou-o. A regra da parte de cada um
   passou a viver num só sítio, `expenseShare` no `irs.ts`, usada pelo Anexo F **e** pela
   página — a única diferença é o `apenasRegistadas`, que é a decisão fiscal de 2026-07-26.
   As colunas dizem agora "(parte)"; o total da família continua por inteiro.
2. **`.limit(5000)` em quatro queries**, a fingir que passa por cima do max-rows (~1000) do
   PostgREST. Não passa — nada passa, é por isso que o `paginateAll` existe. Estavam abaixo
   do corte hoje; passavam a perder linhas em silêncio. Paginadas: pagamentos e recibos de
   12 meses no `load.ts`, recibos do ano no `ano.ts`, recibos e despesas em `/api/irs`.
3. **`conselhos.ts` chamava `new Date()`** apesar de declarar no cabeçalho que o relógio
   entra por parâmetro, e de ter o `hoje` à mão.
4. **O link da ficha do arrendatário aparecia a viewers** com guarda de admin na página: a
   família clicava e era atirada para o Agora. Decisão do utilizador: abrir a ficha a todos.
5. **Três charts mortos** (`MonthlyFlowChart`, `CollectionRateChart`, `FlowLegend`) desde que
   o gráfico mensal saiu do Agora. Apagados: −105 linhas.
6. **`senhorios`/`saude`/`admin` guardavam com um cartão "Área reservada"**, não com
   `redirect("/")` como o CLAUDE.md afirmava. Sem fuga de dados (a query vinha depois), mas
   o repo mentia a quem o lesse. Passaram a redirect.
7. **As despesas do Anexo F ficam todas a 31-12** — decisão do utilizador, mantida: a
   declaração só diz o ANO, e datar ao mês seria precisão inventada (o IMI paga-se em
   prestações, o condomínio é mensal, a conservação é irregular). Consequência a saber:
   `snapshot.fluxo[].liquido` concentra a despesa do ano em dezembro. Hoje é invisível
   (nenhuma superfície mostra o líquido mensal), mas quem voltar a pôr um gráfico de
   líquido mês a mês tem de resolver isto primeiro.

## O que fica por fazer

- Cadernetas do Avô: mesmo script, `python ler_cadernetas.py Avo_Miguel`, quando os PDFs
  existirem. Confirmar se o padrão 1/2 se repete entre Avô e Avó.
- `182321-U-1217-A` é a garagem coletiva do prédio (15 titulares, Pai 1/30) e a área da
  caderneta é a do compartimento inteiro (276,5 m²): o €/m² dessa ficha não quer dizer nada.
- Rústico `182301-R-401` fica sem `area_m2` de propósito (a caderneta só dá hectares de
  terreno, que não é área bruta privativa).

---

# Próxima sessão (planeada 2026-07-26, executada em 2026-07-29)

Duas frentes, ambas de DADOS. Nenhuma precisa de UI nova: as superfícies já estão
construídas e a responder "por saber". O que falta é dar-lhes o que saber.

## A. Cadernetas prediais → fichas das frações

**O que existe:** 30 PDFs em `dados/Pai/caderneta`, todos com o mesmo formato. Os do avô
ainda não foram extraídos e chegam depois.

**Descoberta que muda a estratégia:** os nomes dos ficheiros não são nomes de fração, são
**artigos matriciais** (`182341-U-1362-U`, `182301-R-401`), no formato
freguesia-tipo-artigo-fração que o `parseMatriz` de `irs.ts` já sabe ler. Isso é melhor do
que nomes: `matriz_article` é a chave natural da carteira e tem índice único
(`properties_matriz_uq`). **Juntar por artigo matricial, nunca por nome.**

**Um PDF já foi analisado (2026-07-26).** Duas páginas, texto extraível com pymupdf, sem
OCR. Onde está cada campo, e as armadilhas confirmadas:

| Campo da ficha | Onde está, literalmente | Exemplo |
|---|---|---|
| `dicofre` | `DISTRITO: 18` + `CONCELHO: 17` + `FREGUESIA: 10`, concatenados | `181710` (6 dígitos) |
| `matriz_article` | primeira linha do documento, e confere com o nome do ficheiro | `181710 - U - 1887 - M` |
| `typology` | secção `ELEMENTOS DA FRACÇÃO` → `Tipologia/Divisões:` | `T2` |
| afetação | mesma secção → `Afectação:` | `Habitação` |
| `area_m2` | secção `ÁREAS` **da fração** → `Área bruta privativa:` | `81,3800` |
| `vpt` | `DADOS DE AVALIAÇÃO` → `Valor patrimonial actual (CIMI):` | `€28.726,42` |
| `vpt_year` | mesma secção → `Determinado no ano:` | `2023` |
| quotas | **página 2**, secção `TITULARES` → `Parte:` por NIF | `1/2` |

**ARMADILHA 1, a mais perigosa: há DOIS "área bruta privativa" no documento.** O do prédio
inteiro (`Área bruta privativa total: 2.029,7700 m²`) e o da fração
(`Área bruta privativa: 81,3800 m²`). Uma regex ingénua apanha o primeiro e inflaciona a
área 25 vezes, o que destrói o €/m², o valor estimado e o yield sem dar erro nenhum.
**Ancorar a extração depois de `FRACÇÃO AUTÓNOMA:` / `ELEMENTOS DA FRACÇÃO`**, nunca
procurar no documento todo.

**ARMADILHA 2: o VPT vem em EUROS, não em cêntimos.** `€28.726,42`, formato português
(ponto nos milhares, vírgula nos decimais). É o CONTRÁRIO do CSV do Portal, onde vinha em
cêntimos e se dividia por 100. **Não dividir.**

**ARMADILHA 3: há dois valores patrimoniais na página 1.** O `Valor patrimonial actual
(CIMI): €28.726,42` e o `Vt` da fórmula de avaliação no rodapé (`26.910,00`). O correto é o
**CIMI**, que é o atualizado; o da fórmula é o da avaliação original.

**ARMADILHA 4: encoding.** O texto sai com acentos partidos se não se forçar UTF-8 na
escrita do CSV/SQL. O PDF está bem; é o pipeline que tem de ser explícito.

**Descoberta que vale mais do que a tarefa:** a página 2 tem os **TITULARES com a quota**
(`Tipo de titular: Propriedade plena Parte: 1/2`), por NIF. Neste prédio são o Pai e o Tio,
metade cada um. Isto permite popular e CONFERIR o `property_owners.quota` fração a fração,
que hoje é a origem da anomalia "quotas ≠ 100%" da Saúde dos dados. Extrair também.

**Passos:**
1. `dados/ler_cadernetas.py` com **pymupdf** (já instalado, ver o CLAUDE.md global). Extrai
   por PDF os campos da tabela acima, das duas páginas, respeitando as quatro armadilhas.
2. **Falhar ruidosamente.** Formato igual em todos significa que um campo que não saia é
   um bug do parser, não uma fração sem dados. O script pára e diz qual o PDF, em vez de
   gravar `null`.
3. Saída em duas peças, como o pipeline que já existe (`dados/gerar_sql_import.py`):
   um **CSV para conferência humana** e um **SQL idempotente** só com
   `update properties set ... where matriz_article = ...`. Nunca `insert` de frações: o
   caminho que inventa entidades a partir de uma matriz mal lida está proibido.
4. **Gate:** conferir 3 PDFs à mão contra o CSV antes de colar o SQL. E cruzar a lista de
   30 artigos com os que estão na base: os que não casarem são o sinal de que há frações
   com o artigo mal registado.
5. **Armadilha do VPT:** no CSV do Portal das Finanças o VPT vinha em CÊNTIMOS (÷100). Na
   caderneta em PDF vem em euros. Confirmar numa antes de converter, e não aplicar a
   divisão por hábito.
6. Repetir para o avô quando os PDFs existirem, mesmo script, pasta diferente.

**Desbloqueia:** `marketView().estimatedValue` e `grossYield`, e com eles os conselhos
`yieldBaixo` e `candidatoAVenda` da `/analise`, que hoje devolvem lista vazia.

## B. Despesas por senhorio (a partir do IRS)

**O que o utilizador decidiu:** os valores vêm das declarações de IRS do Pai e do Avô
(a análise do IRS 2025 já está no `PLANO.md`, Apêndice A.4). Para os dois senhorios sem
dados: **Tio espelha o Pai, Avó espelha o Avô**. Nunca haverá dados reais para eles.

**O problema a resolver ANTES de escrever uma linha:** a tabela `expenses` não tem
proveniência. Sem ela, uma despesa copiada do Pai para o Tio fica indistinguível de uma
despesa medida, e a app passa a mostrar "líquido" com ar de facto sobre um número
inventado por espelhamento. Toda a honestidade que a V3 construiu (`Confianca`,
`despesasConhecidas`) morre em silêncio.

**Proposta:**
1. Migração idempotente no fim do `supabase/schema.sql`:
   `alter table expenses add column if not exists origem text not null default 'registada'`
   com `check (origem in ('registada','espelhada','estimada'))`.
2. As despesas do Pai e do Avô entram como `registada`. As do Tio e da Avó entram como
   `espelhada`, com `description` a dizer de quem foram copiadas.
3. `futuro.capacidadeDeInvestimento` passa a contar as espelhadas mas a devolver
   `confianca: "assumido"` quando alguma entra na conta. O Ano mostra a origem.
4. Sem detalhe por fração, entram como despesa geral (`property_id` null), uma linha por
   senhorio, ano e categoria.

5. **DECIDIDO pelo utilizador (2026-07-26):** as despesas espelhadas **ficam fora do Anexo
   F** e de `computeLandlordFiscalYear`. Servem a análise de carteira e a projeção, nunca
   o que se declara ao fisco. A app gera esse quadro para entregar à AT, e deduzir uma
   despesa sem fatura em nome do titular não é a mesma coisa que a estimar para uso
   interno. `anexoFRows` filtra `origem = 'registada'`.

**Nota que só apareceu ao ler a caderneta, e que muda a natureza do espelhamento:** a
página 2 mostra que o Pai e o Tio são titulares da MESMA fração, com `Parte: 1/2` cada um.
Ou seja, espelhar não é analogia solta: o Tio é comproprietário documentado dos mesmos
imóveis, e as despesas desses imóveis são genuinamente partilhadas. Isso sobe a confiança
do número, mas **não altera a decisão do Anexo F**, que é uma questão de prova documental
(quem tem a fatura em nome dele), não de propriedade. Confirmar se o mesmo padrão 1/2 se
repete entre Avô e Avó quando os PDFs dele chegarem.

**Desbloqueia:** o líquido real na projeção da `/analise`, hoje sempre "por saber".

---

## Contrato de um módulo puro da V3

Quem recebe uma tarefa de módulo (`renda.ts`, `futuro.ts`, geradores) entrega:

1. o módulo em `src/lib/portfolio/`, **sem I/O, sem React, sem `new Date()` escondido** — o
   relógio entra sempre por parâmetro, que é o que permite testar com relógio fixo;
2. o `*.check.ts` ao lado, com os casos-limite nomeados;
3. a entrada `check:<mod>` no `package.json`, acrescentada ao script `check`;
4. `npm run build` e `npm run check` verdes.

Proibido: tocar em UI, acrescentar dependências, mexer no schema, reimplementar o que a tabela
acima já dá.

---

## Sessão 2026-07-30 — Documentos por fração

Pedido do utilizador: a página de Documentos ficava com a escolha da fração, mas por baixo
só deviam aparecer **os documentos enquadrados** nessa fração; uma parte de **minutas** e
outra de **informações da fração** (contrato de arrendamento e o resto do arquivo); o
**Geral** passa a admin-only; a página é vista por toda a família e só fala de frações.

- **`enquadrarMinutas()`** em `src/lib/minutas.ts` (puro, `minutas.check.ts`, `check:minutas`
  no script `check` — passam a ser 18). Devolve as cinco cartas SEMPRE, cada uma com
  `bloqueio` (a razão, em PT-PT) ou `null`; quem chama é que esconde. Regras: sem contrato
  ativo não há carta nenhuma (todas identificam um locado e um arrendatário); atualização de
  renda exige elegibilidade **e** coeficiente do ano; interpelação exige rendas por liquidar,
  excluindo os contratos de "ritmo próprio" do `arrears.ts`; oposição à renovação exige três
  anos de contrato (artigo 1097.º n.º 3). **Oferecer uma carta é afirmar que ela se aplica** —
  uma oposição nos primeiros três anos é ineficaz e uma interpelação a quem está em dia é um
  erro que se manda por escrito.
- **A página é uma fração de cada vez**, escolhida no `?fracao=` (o padrão das lentes da
  Carteira: partilhável por link, sobrevive ao F5, e a página continua server component).
  Duas secções, `Seccao` e não `Card`: **Minutas** e **Documentos da fração**. As cartas que
  não se aplicam colapsam para um `<details>` com a razão (R3) — esconder a carta é o pedido,
  esconder o porquê deixava quem procura a atualização de renda sem saber se ela não existe
  ou se a app se enganou.
- **Geral só de admin**, no fim e depois de uma fronteira: não é informação de fração
  nenhuma. Continua a apanhar os órfãos (artigos que nenhuma fração tem), senão desapareciam.
- **Upload com destino fixo** dentro da fração (`<Carregar destino={...}>`, sem seletor); o
  palpite pelo nome do ficheiro e as 30 cadernetas de uma assentada ficam na tira do Geral.
- **`escopoSeguro` dos dois lados** ao casar o bucket com a fração. A página antiga comparava
  o artigo CRU da ficha com o escopo já higienizado do caminho, e por isso a caderneta de
  `182341-U-4260-1ºPES` (o `º` vira `-`) aparecia como órfã no Geral.
- Apagado o `EscolherMinuta` (`components/documentos/acoes.tsx`): escolhia o contrato numa
  lista própria — segunda escolha na mesma página — e oferecia as cinco a toda a gente.

---

## Sessão 2026-07-31 (II) — AIMI a sério, seis anos de Anexo F, e a Carteira mais curta

- **AIMI**: `aimiExposure` somava tudo o que não estivesse vendido ou com `status = 'terreno'`.
  Faltavam as duas exclusões do art. 135.º-B n.º 2 — rústicos (que chegam do Portal como
  "vago", por isso o teste é o `-R-` do artigo) e urbanos de comércio/indústria/serviços. Só
  o `182341-U-2198-A` (Serviços, VPT 310.492 €) valia 1.500 €/ano de imposto imaginário. Novo
  `aimiTax()` com as taxas marginais do art. 135.º-F, e a página do Ano deixa de dizer "a app
  não calcula o AIMI em euros". Casos no `irs.check.ts`.
- **`ler_irs_anexof.py` passa a ler por COORDENADAS.** As páginas do anexo estão rodadas 90°:
  cada linha da tabela é um x constante e cada coluna é um y. O parser lia a sequência de
  tokens, e em 2020-2022 as seis colunas de gastos saem ao contrário — o IMI virava
  condomínio em silêncio. Três descobertas que valem por si: as ETIQUETAS dão a ordem das
  colunas mas não servem de régua (o topo da caixa de "Conservação e manutenção" fica 59
  pontos acima do valor e o de "Imposto do selo" a 27 — a régua é a linha de TOTAIS, que tem
  as seis colunas sempre preenchidas); o artigo matricial **4260** e **4364** casam com o
  padrão do campo `4\d{3}`, por isso o campo é a primeira célula da linha e não "o primeiro
  token que parece um campo"; e a FRACÇÃO também se chama "A" ou "B", como o titular, o que
  só se desfaz porque depois do titular vem sempre a renda.
  Prova de não-regressão: o `despesas_irs_2025.csv` sai **idêntico** ao que já está na base.
  Seis anos lidos, 2020 a 2025, todos a passar o gate dos totais impressos.
- **Carteira com três lentes** (Cobrança, Risco, Vazios) e o ano marcado no primeiro mês do
  eixo, não só nos janeiros.
